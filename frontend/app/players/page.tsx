import { PageHero, SearchFilters, SectionHeader, Shell, EmptyState } from '@/components/site'
import { players } from '@/data/site'
export default function PlayersPage() { return <Shell><PageHero eyebrow="Competition / Players" title="The players to watch." description="A living directory of the talent, roles, and stories shaping the FFWS World Cup."/><section className="content-section"><SearchFilters placeholder="Search players" filters={['All players','Team','Country','Role']}/><SectionHeader eyebrow="Player directory" title="Competitors"/><div className="player-grid">{players.length ? players.map(player => <div key={player.slug}/>) : <EmptyState title="Player profiles are on the way." message="Official rosters will unlock this directory before the tournament begins."/>}</div></section></Shell> }
