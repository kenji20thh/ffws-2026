import { PageHero, SectionHeader, Shell, EmptyState } from '@/components/site'
export default async function MatchDetail({ params }: { params: Promise<{ id: string }> }) { await params; return <Shell><PageHero eyebrow="Match detail" title="Match data, when the action begins." description="This match page is ready for teams, placements, kills, player performances, and room information."/><section className="content-section"><SectionHeader eyebrow="Match information" title="Details to be announced"/><EmptyState title="Match data will appear once the tournament begins." message="Official match information will populate this page after the schedule is published."/></section></Shell> }
