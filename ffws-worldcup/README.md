# FFWS World Cup (frontend v1)
`npm install && npm run dev` then open http://localhost:3000
Demo data lives in `data/`. Replace with API calls later; components only consume the types in `data/types.ts`.
